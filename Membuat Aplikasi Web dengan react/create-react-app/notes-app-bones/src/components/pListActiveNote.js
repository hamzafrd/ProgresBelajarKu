import React from "react";
import ActiveContainer from "./qActiveContainer";

function ListActiveNote(props) {
  return (
    <div className="notes-list">
      <ActiveContainer />
    </div>
  );
}

export default ListActiveNote;
